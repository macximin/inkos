# Hermes × InkOS 대량 사용 계획

- 작성일: 2026-08-11
- 대상: `/Users/a2501/Desktop/inkos`
- 상태: **계획 문서 — 아직 Hermes 설정, 스케줄러, 서비스, 작업 큐를 만들거나 실행하지 않음**

## 1. 목적과 범위

Hermes를 이용해 여러 작품의 한국어 웹소설 제작 작업을 안정적으로 반복 처리한다. InkOS는 작품의 기획·Arc·원고·감리·수정·상태를 소유하고, Hermes는 승인된 작업을 순서대로 호출하고 결과를 수집하는 실행 운영 계층을 맡는다.

대상 흐름은 다음과 같다.

```text
작품별 제작계획 승인
  → Hermes 작업 카드 생성
  → InkOS 실행 요청
  → 실제 산출물·상태·감리 결과 확인
  → 사람 검토 또는 다음 작업 해제
```

이번 계획에서 제외한다.

- Hermes 자동 실행, launchd/cron 등록, 프로필 또는 비밀값 수정
- InkOS 코드·설정·작품 파일 변경
- 기존 작업 트리의 변경사항 정리, 스테이징, 커밋, 푸시
- 한 작업에서 여러 작품 또는 동일 작품의 여러 회차를 무제한 병렬 실행

## 2. 현재 기반과 전제

| 항목 | 현재 확인된 기반 | 운영상 의미 |
| --- | --- | --- |
| 외부 호출 진입점 | `inkos interact --json --message "…"` | Hermes는 임의의 여러 CLI 명령을 조합하기보다 이 공통 진입점을 우선 사용한다. |
| 실제 완료 판정 | InkOS 도구 결과와 파일이 진실 기준 | 모델의 완료 문장만으로 작업 성공을 처리하지 않는다. |
| 작품 단위 동시성 | daemon 기본값 `maxConcurrentBooks=3`, 동일 세션은 내부 큐 처리 | 대량 실행도 작품 간 병렬성만 제한적으로 허용하고, 동일 작품은 직렬화한다. |
| 원고 처리 | `inkos auto`는 회차별 전체 파이프라인과 자동 감리·수정을 수행하며 오류/상태저하 시 중단 | 배치 하나의 실패가 다음 회차로 전파되지 않도록, 작업 단위를 작게 유지한다. |
| 장애 대응 | 일시적 네트워크·429·502/503/504에는 제한 재시도 | Hermes 자체 재시도는 InkOS 내부 재시도와 중복 폭주하지 않도록 별도 정책이 필요하다. |
| 한국어 집필 모델 | `기획서 → Arc(1~3화) → 원고` | 대량 생산 단위는 ‘N화’가 아니라 검토 가능한 Arc여야 한다. |

사전 확인이 필요한 미확정 사항도 있다.

- Hermes의 현재 프로필, 작업 카드 형식, 재시도·알림 기능, 실행 계정과 설치된 `inkos` 경로
- Hermes가 장시간 프로세스의 취소 신호와 표준 출력/오류를 보존하는 방식
- 각 작품의 모델 공급자 한도, 비용 한도, 사용 가능한 Codex 로그인 상태
- 실제 운영할 작품 수, 회차 길이, 하루 산출량, 사람 검토 가능량

이 항목들은 구현 시작 전에 읽기 전용으로 확인하고, 비밀값은 표시하거나 문서에 복사하지 않는다.

## 3. 책임 경계

```text
Hermes
  - 승인된 작업의 대기열·우선순위·단일 실행 락
  - InkOS 호출, 실행 식별자·로그 위치·종료 코드 기록
  - 결과 수집, 보류/재시도/사람 검토 알림

InkOS
  - 작품 정본, 기획서, Arc, 회차 원고, 감리·수정·상태 파일
  - 회차 생성 및 저장의 잠금·복구·검증
  - 모델 호출과 내부 일시 오류 재시도

사람(저자/편집자)
  - 작품 목표·Arc 승인, 품질·연속성 최종 판정
  - 재시도/재작성/다음 Arc 진행 승인
```

Hermes는 원고·정본·상태 파일을 직접 고치지 않는다. 수정이 필요하면 InkOS에 의도를 전달하고, InkOS가 남긴 결과를 다시 확인한다.

## 4. 작업 카드 계약(초안)

초기에는 한 카드가 **한 작품의 한 Arc 또는 한 회차 감리/수정**만 다룬다. 같은 작품의 다음 카드는 앞 카드가 성공하고 필요한 검토를 통과한 뒤에만 해제한다.

필수 필드:

```text
job_id                 불변 식별자
book_id                InkOS 작품 ID
arc_id / chapter_no    목표 Arc 또는 회차 (둘 중 최소 하나)
operation              plan | draft | review | revise | continue
author_brief           이번 작업의 저자 지시
priority               실행 순서
idempotency_key        중복 실행 방지 키
created_at / deadline  생성·마감 시각
approval_state         queued | approved | blocked | cancelled
attempt                시도 횟수
```

실행 뒤 Hermes가 기록할 필드:

```text
started_at / ended_at
inkos_command_version
interaction_session_id (존재할 때)
exit_code
result_receipt_paths   InkOS가 실제로 남긴 산출물·상태·감리 결과 경로
outcome                succeeded | needs_review | retryable_failure | blocked | failed
failure_class          configuration | capacity | transient | quality | state_integrity | unknown
next_action
```

작업 카드에는 API 키, 로그인 토큰, 원문 전체, 모델 응답 전문을 넣지 않는다. 필요한 저자 지시와 산출물 경로·요약만 보존한다.

## 5. 실행 정책

### 5.1 동시성 및 순서

1. 동일 `book_id`는 항상 1개만 실행한다.
2. 초기 파일럿은 전체 동시 실행 1개로 시작한다.
3. 안정화 후에도 작품 간 동시 실행은 2개, 최대 3개를 넘기지 않는다. InkOS 기본 daemon 한도(3)를 운영 상한의 근거로만 사용하며, Hermes가 이를 자동으로 보장한다고 가정하지 않는다.
4. Arc 안의 회차는 순서대로 실행한다. 다음 회차는 이전 회차의 산출물·상태·감리 결과가 확인된 뒤에만 열린다.
5. 긴 배치 목표가 있어도 1회 실행은 최대 1개 Arc(1~3화)로 쪼갠다.

### 5.2 성공과 보류

`exit code=0`만으로 성공 처리하지 않는다. 다음을 모두 충족할 때만 `succeeded` 또는 `needs_review`로 전환한다.

1. InkOS가 요청한 작업에 대응하는 실제 결과를 반환했다.
2. 기대한 원고/계획/감리 산출물과 상태 변경이 해당 작품 내부에 존재한다.
3. `state-degraded`, 잠금 충돌, 중단, 미해결 치명 감리 문제가 없다.
4. 다음 카드가 읽을 수 있도록 산출물 경로와 요약이 기록됐다.

원고가 만들어졌지만 편집자 확인이 필요한 경우는 `needs_review`다. 성공으로 닫고 다음 Arc를 자동 해제하지 않는다.

### 5.3 재시도와 중단

- 429, 일시 네트워크 단절, 502/503/504는 InkOS의 제한 재시도가 먼저 끝난 뒤 Hermes에서 지수 백오프로 최대 2회만 재시도한다.
- 설정 오류, 로그인 부재, 모델 미제공, 작업 카드 모호성, 정본 상태 손상은 즉시 `blocked`로 옮기며 자동 재시도하지 않는다.
- 품질/연속성/감리 실패는 `needs_review` 또는 `blocked`다. 같은 프롬프트를 반복해 통과시키지 않는다.
- 실행 중 취소, 타임아웃, 강제 종료 뒤에는 재실행 전 반드시 작품의 잠금·최근 산출물·상태를 읽어 중복 저장 여부를 확인한다.

## 6. 단계별 도입 계획

| 단계 | 목표 | 실행 범위 | 다음 단계 진입 조건 |
| --- | --- | --- | --- |
| 0. 읽기 전용 적합성 확인 | Hermes와 InkOS의 실제 접점 확정 | Hermes 실행 환경, InkOS 설치/로그인, 한 작품의 상태·락·결과 경로 확인 | 비밀값 없이 호출·취소·로그 수집 경로를 설명할 수 있음 |
| 1. 수동 단건 리허설 | 카드 계약과 판정 절차 검증 | 비운영/복제 작품에서 1개 계획 또는 감리 카드 | 산출물·상태·실패 기록을 사람이 재현 가능하게 확인 |
| 2. 제한 파일럿 | 실제 제작 흐름 검증 | 1개 작품, 1개 Arc, 전체 동시성 1 | 중복 없음, 상태저하 없음, 사람 검토 후 결과 수용 |
| 3. 반복 운영 | 작가의 검토 리듬에 맞춘 정기 실행 | 2~3개 작품, 작품별 직렬, Arc 단위 | 2주간 오류·재시도·검토 적체가 허용 범위 이내 |
| 4. 선택적 확장 | 필요한 병목만 개선 | 대기열 우선순위, 알림, 비용·품질 지표 | 확장 전후 품질 저하·비용 증가·검토 적체를 비교해 승인 |

각 단계는 별도 승인점이다. Hermes 스케줄을 상시 활성화하는 일은 0~2단계의 검증을 건너뛰어 진행하지 않는다.

## 7. 관측 지표와 운영 보고

매 실행 종료 시 한 줄 요약을 남긴다.

```text
[job_id] [book_id] [arc/chapter] outcome | attempts | duration | receipt | next_action
```

일간 또는 주간 운영에서는 아래 수치만 집계한다.

- 처리 카드 수: 성공 / 검토 대기 / 재시도 / 차단 / 취소
- 작품별 생성 회차와 Arc 완료 수
- 평균·p95 실행 시간, 재시도율, 중단율
- 감리 통과율과 사람 검토에서의 반려율
- 상태저하·락 충돌·중복 실행 건수 (목표: 0)
- 모델별 호출 실패·제한 응답 비율 및 비용(측정 가능할 때만)
- 검토 대기열의 가장 오래된 카드 경과 시간

수치는 원고의 품질을 대체하지 않는다. 품질 저하, 캐릭터 붕괴, Arc 약속 미이행, 편집자 검토 적체가 보이면 처리량을 낮추고 원인을 먼저 고친다.

## 8. 구현 착수 전 체크리스트

- [ ] 현재 Hermes 프로필과 기존 스케줄 작업을 읽기 전용으로 목록화했다.
- [ ] Hermes의 실행 계정에서 `inkos` 버전·경로와 Codex 로그인 상태를 비밀값 없이 확인했다.
- [ ] 운영 작품과 테스트 작품의 디렉터리·권한·백업 위치를 분리했다.
- [ ] 작업 카드 저장 위치와 보존 기간을 정했고, 작품 정본과 분리했다.
- [ ] 동일 작품 중복 실행을 막는 `book_id` 락과 `idempotency_key` 규칙을 구현·시험했다.
- [ ] 종료 코드뿐 아니라 InkOS 결과 파일·상태·감리 결과를 판정하는 검증기를 구현·시험했다.
- [ ] 취소·타임아웃·부분 성공·상태저하·429의 재개 절차를 리허설했다.
- [ ] 저자/편집자 검토 대기 상태와 다음 Arc 해제 권한을 명확히 했다.
- [ ] 상시 스케줄 활성화 전 파일럿 결과를 사람이 승인했다.

## 9. 첫 구현 작업의 권장 순서

1. Hermes와 InkOS의 현 상태를 읽기 전용으로 조사하고, 이 문서의 미확정 항목을 사실로 교체한다.
2. 정본과 분리된 테스트 프로젝트에서 작업 카드 스키마·락·결과 검증기만 만든다.
3. 한 개의 비파괴적 계획/감리 작업으로 JSON 수집과 영수증 판정을 시험한다.
4. 1개 작품의 1개 Arc 파일럿을 저자 검토와 함께 진행한다.
5. 결과가 충분할 때에만 Hermes의 정기 실행과 다작품 동시성을 별도 승인으로 추가한다.

## 10. 기준 문서와 코드 위치

- 한국어 웹소설 제품 결정 및 Arc 계약: `WEBNOVEL_KO_IMPLEMENTATION_CONTEXT.md`
- 외부 에이전트 공통 진입점과 완료 판정 원칙: `README.md`, `skills/SKILL.md`
- 무인 회차 배치의 중단·상태저하 처리: `packages/cli/src/commands/auto.ts`
- daemon의 작품 동시성·주기 설정: `packages/cli/src/commands/daemon.ts`, `packages/core/src/models/project.ts`
- 동일 세션 직렬 큐: `packages/core/src/agent/agent-session.ts`
- 일시적 모델 오류 재시도 범위: `packages/core/src/llm/provider.ts`

---

이 문서는 운영 설계의 기준선이다. 구현 시에는 실제 Hermes 인터페이스와 InkOS 실행 결과를 먼저 재검증하고, 여기의 미확정 가정을 코드나 자동 실행으로 옮기지 않는다.
